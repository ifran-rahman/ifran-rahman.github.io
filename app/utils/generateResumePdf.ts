// import jsPDF from 'jspdf';

export function generateResumePdf() {
  //   const doc = new jsPDF();
  //   doc.text('Resume Content', 10, 10); // Customize with actual resume data
  //   // Add more resume details here, if needed.
  //   doc.save('resume.pdf');
}
